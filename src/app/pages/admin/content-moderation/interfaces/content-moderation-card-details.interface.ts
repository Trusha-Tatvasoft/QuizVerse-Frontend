import { CardColor } from '../../../../utils/types/card-component.type';

export interface ContentModerationCardDetails {
  title: string;
  icon: string;
  subtitle: string;
  iconColor: CardColor;
}
