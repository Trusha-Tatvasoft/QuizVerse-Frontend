import { ButtonConfig } from '../../../../shared/interfaces/button-config.interface';

export const quickBattleButtonConfig: ButtonConfig = {
  label: 'Quick Battle',
  variant: 'secondary',
  matIcon: 'flash_on',
  imagePosition: 'left',
};

export const browseQuizzesButtonConfig: ButtonConfig = {
  label: 'Browse Quizzes',
  variant: 'secondary',
  matIcon: 'menu_book',
  imagePosition: 'left',
};
