export enum UserRoles {
  Admin = 1,
  Player = 2,
  SuperAdmin = 3,
}

export enum UserStatus {
  Active = 1,
  Inactive = 2,
  Suspended = 3,
}

export enum UserAction {
  Delete = 1,
  UpdateStatus = 2,
}
