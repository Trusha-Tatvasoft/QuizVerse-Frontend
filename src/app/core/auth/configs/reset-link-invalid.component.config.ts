import { ButtonConfig } from '../../../shared/interfaces/button-config.interface';

export const newResetLinkButton: ButtonConfig = {
  label: 'Request New Reset Link',
  fontWeight: 500,
  variant: 'gradient',
  type: 'submit',
};

export const backToSignInButton: ButtonConfig = {
  label: 'Back to Sign In',
  fontWeight: 500,
  variant: 'secondary',
  type: 'button',
};
