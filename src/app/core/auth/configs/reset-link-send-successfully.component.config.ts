import { ButtonConfig } from '../../../shared/interfaces/button-config.interface';

export const backToSignInConfig: ButtonConfig = {
  label: 'Back to Sign In',
  fontWeight: 500,
  variant: 'gradient',
  type: 'submit',
};

export const sendAnotherEmailConfig: ButtonConfig = {
  label: 'Send Another Email',
  fontWeight: 500,
  variant: 'secondary',
  type: 'button',
};
