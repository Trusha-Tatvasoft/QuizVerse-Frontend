import { ButtonConfig } from '../../../shared/interfaces/button-config.interface';

export const BACK_TO_SIGNIN_CONFIG: ButtonConfig = {
  label: 'Back to Sign In',
  fontWeight: 500,
  variant: 'gradient',
  type: 'submit',
};

export const SEND_ANOTHER_EMAIL_CONFIG: ButtonConfig = {
  label: 'Send Another Email',
  fontWeight: 500,
  variant: 'secondary',
  type: 'button',
};
